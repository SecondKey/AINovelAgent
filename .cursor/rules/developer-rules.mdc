---
description:编码时应该严格遵守编码规范（文档/编码规范.md）
globs:
alwaysApply: false
---

每次编码任务完成后，需要执行dotnet build命令来执行构建并解决编译错误。当整个模块构建完成后，需要调用puppet命令（不是dotnet run）来测试功能是否正常。当某个功能切换为另一种实现，且测试正常后，应将前一种实现的所有剩余未使用代码清除。