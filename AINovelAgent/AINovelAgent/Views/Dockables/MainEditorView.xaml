<local:DockableContentBase x:Class="AINovelAgent.Views.Dockables.MainEditorView"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:local="clr-namespace:AINovelAgent.Base"
			 mc:Ignorable="d"
			 Title="编辑器" ContentId="Document.Editor">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <ToolBar KeyboardNavigation.IsTabStop="False">
            <ComboBox Width="140" SelectedIndex="0" KeyboardNavigation.IsTabStop="False">
                <ComboBoxItem Content="Consolas" />
                <ComboBoxItem Content="Segoe UI" />
                <ComboBoxItem Content="Microsoft YaHei" />
            </ComboBox>
            <ComboBox Width="60" SelectedIndex="2" KeyboardNavigation.IsTabStop="False">
                <ComboBoxItem Content="10" />
                <ComboBoxItem Content="12" />
                <ComboBoxItem Content="14" />
                <ComboBoxItem Content="16" />
                <ComboBoxItem Content="18" />
            </ComboBox>
            <Separator />
            <ToggleButton Content="B" FontWeight="Bold" Width="28" KeyboardNavigation.IsTabStop="False" />
            <ToggleButton Content="I" FontStyle="Italic" Width="28" KeyboardNavigation.IsTabStop="False" />
            <ToggleButton Width="28" KeyboardNavigation.IsTabStop="False">
                <TextBlock Text="U" TextDecorations="Underline" />
            </ToggleButton>
            <Separator />
            <Button Content="撤销" KeyboardNavigation.IsTabStop="False" />
            <Button Content="重做" KeyboardNavigation.IsTabStop="False" />
            <Separator />
            <Button Content="左对齐" KeyboardNavigation.IsTabStop="False" />
            <Button Content="居中" KeyboardNavigation.IsTabStop="False" />
            <Button Content="右对齐" KeyboardNavigation.IsTabStop="False" />
        </ToolBar>
        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <!-- 编辑区域：仅文本框 -->
            <TextBox Grid.Row="0" Name="EditorBox" AcceptsReturn="True" VerticalScrollBarVisibility="Auto" TextWrapping="Wrap"
                        FontFamily="Consolas" FontSize="14" TextChanged="EditorBox_TextChanged" SelectionChanged="EditorBox_SelectionChanged" PreviewKeyDown="EditorBox_PreviewKeyDown" />
            <!-- 本地状态栏：左下角显示行列号 -->
            <StatusBar Grid.Row="1">
                <StatusBarItem>
                    <TextBlock Name="EditorStatusTextBlock" Text="行 1, 列 1" />
                </StatusBarItem>
            </StatusBar>
        </Grid>
    </Grid>
</local:DockableContentBase>